"""Generation layer — deterministic synthetic RoI data generator.

Byte-identical across 3 runs (determinism contract). Supports `--inject-violation`
to produce valid/violating fixture pairs per rule-ID.
"""
